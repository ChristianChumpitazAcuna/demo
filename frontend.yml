---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: 07christianchumpitaz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: christianca/vg-ms-profile_fe:v2
          ports:
            - containerPort: 4200
          resources:
            requests:
              cpu: 125m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: 07christianchumpitaz
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 4200
      targetPort: 4200
  type: LoadBalancer
